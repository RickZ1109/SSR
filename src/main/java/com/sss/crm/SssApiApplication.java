package com.sss.crm;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication /* <-- Marca a classe principal em um aplicativo Spring Boot, indicando que ela contém a configuração e inicialização do aplicativo.*/
public class SssApiApplication {

	public static void main(String[] args) {
		SpringApplication.run(SssApiApplication.class, args);
	}

}
